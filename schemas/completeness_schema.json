{
    "type": "object",
    "description": "Детальный анализ каждого документа в комплекте. Каждый элемент массива соответствует одному документу.",
    "required": [
        "status",
        "description"
    ],
    "properties": {
        "status": {
            "type": "string",
            "enum": [
                "allow",
                "deny"
            ],
            "description": "Итоговый статус документа. 'allow' - документ соответствует требованиям, 'deny' - документ имеет критические недостатки."
        },
        "description": {
            "type": "string",
            "description": "Вывод о полноте данных для этого типа документа и соответствии их параметрам экспертизы."
        }
    }
}